# 이 저장소에서 일할 때

## 토론 태도 — 동의는 결론이지 출발점이 아니다

Jay가 반복해서 요구한 것이고, 파이프라인 에이전트들에게는 이미 걸려 있는 규칙이다
(`journal_agent.py`의 "전제에 아부하지 마세요", `discussion.py`의 비판적 검토자,
`weekly.py`의 "아부 금지", `analysis_agent.py`의 양방향 반증 조건). 대화하는 나에게도
똑같이 적용한다.

- Jay의 시장 해석에 동의할 때도 **같은 그림을 만드는 다른 원인**을 함께 제시한다.
  예: 장기금리 상승은 인플레 기대일 수도, 국채 수급·텀프리미엄일 수도 있다 —
  구분하는 관측치(BEI, 실질금리)를 같이 말한다.
- 데이터가 Jay의 말과 어긋나면 **어긋나는 부분을 먼저 짚는다.** 맞는 부분을 길게
  확인한 뒤 반박을 덧붙이면 반박이 묻힌다.
- "좋은 지적입니다" 같은 칭찬으로 문단을 열지 않는다. 검증 결과부터 쓴다.
- 반대 시나리오가 떠오르지 않으면 그렇다고 말한다. 억지 반론은 아부보다 나쁘다.

## 사실과 추측을 섞지 않는다

- 이 샌드박스는 egress 프록시가 외부 대부분을 막는다(cboe·fred·naver·stooq·
  docs.google.com 등 확인됨). **차단과 코드 결함을 구분해서 보고한다.** 우회하지 않는다.
- 확인하지 못한 것은 "확인 못 했다"고 명시한다. 과거에 DRAMeXchange를 유료라고
  단정했다가 틀렸고, 게시 시트를 "다른 문서"라고 단정했다가 근거가 토큰 비교뿐임을
  나중에 밝혔다. 단정 전에 근거를 댈 수 있는지 확인한다.
- 마크업·응답 구조를 보지 못한 채 파서를 쓰지 않는다. 대신 `--check` 프로브로
  실제 응답을 로그에 남겨 다음 실행에서 판별한다.

## 조용한 실패를 찾는다

이 시스템에서 가장 비싼 버그는 예외가 아니라 **침묵**이었다:

- `yield_curve.py`가 Yahoo 심볼로만 만기를 찾아 금리 커브 섹션이 한 번도 출력되지
  않았다. 2s10s는 2년물이 아예 없어 영영 계산 불가였다.
- `rebound.py`가 조정 없는 종목에 `None`을 줘서 신고가 종목(PLTR·MSFT)이 목록에서
  사라졌다.
- `today_insights()`가 Actions에서 항상 빈 문자열이었다(`discussions/`가 gitignore).

그래서: 신호가 안 보이면 "오늘은 신호가 없나 보다"로 넘기지 말고 **왜 안 나오는지
코드에서 확인한다.** 무언가를 목록에서 제외할 때는 제외 이유를 한 줄이라도 남긴다.

## 값이 조용히 틀리는 경우를 경계한다

숫자가 그럴듯하면 틀려도 아무도 모른다. 실제로 잡은 것들:

- 게시 CSV에서 **전일비를 현재가로** 읽을 뻔했다(둘 다 숫자라 값으로는 구분 불가).
  → 헤더 행을 읽어 열을 정한다.
- CBOE 금리 ÷10 보정이 `>15`에서만 걸려 저금리 구간에 10배 오류가 났다.
- 금 시세 JSON에서 **은값(`xagPrice`)을 금값으로** 집었다.

새 파서를 쓸 때는 이런 종류의 케이스를 테스트에 먼저 넣는다.

## 작업 규칙

- 브랜치 `claude/investment-journal-maintenance-iv2mef`에서 작업하고 PR로 머지한다.
  cron은 main에서만 돈다.
- 커밋 메시지는 무엇을 고쳤는지가 아니라 **왜 그게 문제였는지**를 적는다.
- 시트·Secrets 변경은 Jay만 할 수 있다. 순서가 중요하면(예: 파서 머지 → URL 교체)
  명시한다.
- 검증은 실제 `market_history/` 데이터로 한다. 가짜 데이터만 쓰면 실제 키 형식
  (`gsheet/INDEXCBOE:TNX`)과 어긋나 정작 그 버그를 놓친다.

## 시스템 지도 (요약)

- 아침 브리핑(KST 화~토 06:00) → Notion. `analysis_agent` + `signals/`
- 저녁 메모 페이지(평일 17:50) → 밤 자동 일지(21:40). 메모·토론 둘 다 없으면 건너뜀
- 토론은 Streamlit 앱, 기록은 Notion(`discussion_store`) — 폰과 Actions가 공유
- 시세는 구글시트 게시 CSV(`sheet_source`), 금은 gold-api 직접 수집
- 워치리스트 `portfolio.md`, 장기 전제 `thesis.md`
- 진단: `/invest:sheet`(시트 수신 점검), `/invest:signals`(침묵 신호 찾기), `--check`
